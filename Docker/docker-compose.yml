# NOTA TLS: se despliega temporalmente SIN HTTPS (trafico en claro).
# Antes de exponer credenciales reales, colocar un proxy TLS
# (Caddy/nginx o el host) delante del puerto 8080.
services:
  db:
    image: postgres:16
    container_name: postgresDB_Gradox2
    restart: always
    env_file:
      - .env
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 5
    mem_limit: 768m
    mem_reservation: 512m
    cpus: 1.0
    profiles: ["db", "full"]

  app:
    build:
      context: ..
      dockerfile: Docker/Dockerfile
    container_name: gradox2_app
    depends_on:
      db:
        condition: service_healthy
    restart: unless-stopped
    env_file:
      - .env
    environment:
      SPRING_DATASOURCE_URL: ${SPRING_DATASOURCE_URL}
      SPRING_DATASOURCE_USERNAME: ${SPRING_DATASOURCE_USERNAME}
      SPRING_DATASOURCE_PASSWORD: ${SPRING_DATASOURCE_PASSWORD}
      JWT_SECRET: ${JWT_SECRET}
      JWT_EXPIRATION: ${JWT_EXPIRATION}
      SPRING_MAIL_HOST: ${SPRING_MAIL_HOST}
      SPRING_MAIL_PORT: ${SPRING_MAIL_PORT}
      SPRING_MAIL_USERNAME: ${SPRING_MAIL_USERNAME}
      SPRING_MAIL_PASSWORD: ${SPRING_MAIL_PASSWORD}
      SPRING_PROFILES_ACTIVE: ${SPRING_PROFILES_ACTIVE}
      APP_BASE_URL: ${APP_BASE_URL}
      LOG_DIR: /app/logs
      BOOTSTRAP_MASTER_USERNAME: ${BOOTSTRAP_MASTER_USERNAME}
      BOOTSTRAP_MASTER_PASSWORD: ${BOOTSTRAP_MASTER_PASSWORD}
      BOOTSTRAP_MASTER_EMAIL: ${BOOTSTRAP_MASTER_EMAIL}
    ports:
      - "8080:8080"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/actuator/health"]
      interval: 30s
      timeout: 5s
      start_period: 60s
      retries: 3
    mem_limit: 1024m
    mem_reservation: 768m
    cpus: 1.0
    profiles: ["full"]

volumes:
  pgdata:
